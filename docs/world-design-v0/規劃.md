這一批我現在的判斷很明確：

# **主體放 `C:\ai_agent\volva`，不是 Thyra。**

而且不是 55 開，
是大概 **80% 放 Völva，20% 只在 Thyra 留接口與消費點**。

---

## 為什麼？

因為你現在這批東西在解的是：

- 使用者到底在問什麼
- 這是什麼 intent regime
- realization path 固定了沒
- 要不要先長 search space
- 怎麼 probe / commit
- 什麼時候才配進 Forge
- 什麼時候才配進 Thyra

這整塊本質上不是 governance runtime，
而是：

> **前置理解、收斂、分流、路由**

而這剛好就是 Völva 在你現有定義裡的核心：

- 對話理解與意圖收斂層
- Short Card system
- Settlement router
- 人類直覺 → 結構化產物 → 分流到 Thyra / Karvi

你剛剛這批 spec，本質上就是把 Völva 的「理解層」從普通 intent parser，往前推到**真正的 decision routing layer**。

---

# 最短答案

## 放置原則

### 放 **Völva** 的
凡是回答這些問題的，都放 Völva：

- 你現在是哪種 intent？
- realization path 固定了沒？
- 要不要先生成 space？
- space 裡有哪些 candidate？
- 怎麼 probe / filter / commit？
- 什麼時候交給 Forge？
- 什麼時候交給 Thyra？

### 放 **Thyra** 的
凡是回答這些問題的，才放 Thyra：

- world form 已選定後，怎麼 instantiate？
- live world 的 state / change / judgment / pulse / outcome 怎麼跑？
- chiefs / laws / rollback / precedent-fed governance 怎麼運作？

---

# 所以具體來說，你剛剛寫的這些檔案，我會這樣分

---

## 應該放 `C:\ai_agent\volva`
這些幾乎全部都應該放在 Völva：

- `intent-router.md`
- `path-check.md`
- `intent-router-and-space-builder.md`
- `probe-commit.md`
- `probe-commit-evaluators.md`
- `regime-comparison-matrix.md`
- `router-test-cases.md`
- `economic-regime-v0.md`

### 還有這個也放 Völva：
- `governance-regime-v0.md`

這個雖然名字有 governance，
但它現在處理的不是 Thyra 內部治理邏輯，
而是：

> **在還沒進 Thyra 之前，怎麼選出第一個值得被治理的 world form。**

這仍然是前置 routing / selection 問題，
所以主體還是 Völva。

---

## 應該放 `C:\ai_agent\thyra`
這些才是 Thyra 主場：

- `canonical-cycle.md`
- `change-proposal-schema-v0.md`
- `judgment-rules-v0.md`
- `world-cycle-api.md`
- `midnight-market-canonical-slice.md`
- `midnight-market-demo-path.md`
- `pulse-and-outcome-metrics-v0.md`

因為這批在定義的是：

- world 已經成立之後
- change 怎麼被制度化
- cycle 怎麼跑
- pulse / outcome 怎麼形成
- world runtime 怎麼運行

這是 Thyra 的心臟，不是 Völva 的。

---

# 更精準一點講

## Völva 負責的是：
# **「該不該進世界」之前**

它處理的是：
- 聽懂人
- 把終局意圖分類
- 補出缺資訊
- 長出 realization space
- 找到該走哪個路徑
- 決定是不是該進 Forge / Thyra

這是**前置編譯層**。

---

## Thyra 負責的是：
# **「世界已決定要存在」之後**

它處理的是：
- world state
- change
- judgment
- chiefs
- pulse
- outcomes
- precedent-fed governance

這是**runtime 治理層**。

---

# 所以如果你問的不是 repo，而是概念邊界
我會這樣切：

## Völva
**intent → route → shape → commit**

## Thyra
**instantiate → govern → adapt**

這樣最乾淨。

---

# 為什麼不是放 Thyra？
因為如果你把這批 intent / regime / space-builder 放進 Thyra，會有三個壞處。

## 壞處 1：Thyra 會被拉回「前台理解器」
Thyra 本來應該很硬：
- laws
- chiefs
- world state
- judgment
- rollback

如果把太多 intent routing 放進去，
它會開始變成：
- 理解自然語言
- 推測使用者想做什麼
- 幫忙想 realization space

這會讓 Thyra 變軟。

---

## 壞處 2：world runtime 和 pre-world routing 會混在一起
這兩層其實完全不同：

### pre-world
- 這要不要成為 world？
- world form 是 market 還是 town？
- 還要不要先 probe？

### live-world
- 現在 north gate 要不要限流？
- 這條 law 要不要修？
- 這個 change 要不要 rollback？

你把它們混在一起，
架構會很快失焦。

---

## 壞處 3：Economic / Identity 這些根本不屬於 Thyra
這點最關鍵。

你現在這前置層，不只要接 governance intent，
還要接：

- economic
- capability
- leverage
- expression
- identity

這些裡面很多最後甚至不一定進 Thyra。
如果一開始就把入口做在 Thyra，會讓 Thyra 無端背了很多不屬於它的問題。

---

# 那為什麼不是全部都放 Völva 就好？
因為又會走太遠。

Völva 不該擁有這些東西的 runtime 實作：

- world state
- judgment engine
- change grammar
- pulse engine
- outcome engine
- precedence-based governance

它應該只做：

- route
- card
- settlement
- handoff

也就是：

> **Völva 決定要交給誰，不自己成為那個誰。**

---

# 所以我現在會建議一個很具體的落法

## 在 `C:\ai_agent\volva\docs\` 下新增一個新區塊
例如：

```text
C:\ai_agent\volva\docs\INTENT\
```

或如果你想更貼現在語意：

```text
C:\ai_agent\volva\docs\ROUTING\
```

我比較偏 `INTENT`，因為比較貼 Völva 的角色。

### 這裡放：
- `intent-router.md`
- `path-check.md`
- `intent-router-and-space-builder.md`
- `probe-commit.md`
- `probe-commit-evaluators.md`
- `regime-comparison-matrix.md`
- `router-test-cases.md`
- `economic-regime-v0.md`
- `governance-regime-v0.md`

---

## 在 Thyra docs 裡只留一個橋接文件
例如可以新增：

```text
C:\ai_agent\thyra\docs\THYRA\T0_VOLVA_HANDOFF.md
```

裡面只講：

- Thyra 接收什麼 handoff object
- 哪些東西必須先由 Völva 決定
- 什麼條件下，governance candidate 才能進 Thyra
- Völva → Thyra 的 canonical handoff schema 是什麼

這樣 Thyra 知道自己接的是什麼，
但不需要自己承擔 intent routing。

---

# 如果再往更硬一點的工程切法講

## Völva 應有：
- `intent-router`
- `path-check`
- `space-builder`
- `probe-commit`
- `settlement-router`

## Thyra 應有：
- `world-kernel`
- `change-engine`
- `judgment-engine`
- `cycle-runner`
- `pulse-engine`
- `outcome-engine`

這是兩邊最健康的邊界。

---

# 最終判斷

## 你現在這個「意圖層」
### **應該放在 `C:\ai_agent\volva`**
因為它本質上是：
- conversation-side routing
- pre-world search
- pre-build commitment

### **不要主體放在 Thyra**
因為 Thyra 應該從：
- world 已經選定
- form 已經定下
- closure 已經被授權
之後才開始接手。

---

# 我幫你壓成最後一句

> **Völva 負責決定「這是不是一個世界、該長成什麼樣、現在配不配進入世界治理」；Thyra 負責接手「已被選中的世界」，讓它真的活起來並被治理。**

所以這一批，主體放 **`C:\ai_agent\volva`**。

如果你要，我下一步可以直接幫你做兩件事其中一個：

1. **幫你規劃 `volva/docs/INTENT/` 的資料夾結構**
2. **直接寫一份 `volva ↔ thyra handoff contract`**

我建議先做 **2**，因為這樣兩邊邊界會一次釘死。

####

放 **`C:\ai_agent\volva`**。

更精準地說：

# 放置結論

## 放在 `C:\ai_agent\volva\docs\...`
這批都屬於 Völva：

- `intent-router.md`
- `path-check.md`
- `intent-router-and-space-builder.md`
- `probe-commit.md`
- `probe-commit-evaluators.md`
- `regime-comparison-matrix.md`
- `router-test-cases.md`
- `economic-regime-v0.md`
- `governance-regime-v0.md`

因為它們都在做同一件事：

> **把人的自然語言意圖，路由成後續該進哪種搜索空間、哪種 probe、何時才配進 Forge / Thyra。**

這正是 Völva 現在文件裡的定位：

- 對話理解
- 意圖收斂
- settlement router
- 把人話翻成結構化下一步

---

# 不放 Thyra 的原因

因為 Thyra 的核心已經很清楚是：

- world state
- change proposal
- judgment
- rollback
- pulse
- outcome
- precedent-fed governance

也就是：

> **世界已經選定之後，怎麼讓它活起來並被治理。**

你現在這批不是在處理「已選定世界如何運轉」，  
而是在處理：

- 這到底是哪種 intent
- realization path 固定了沒
- 要不要先長候選空間
- 哪種 regime 的 probe/commit 判準要上場

這些都還在 **pre-world / pre-build**。

---

# 一句最短區分

## Völva
**決定要不要進世界、進哪種世界、現在配不配進 build。**

## Thyra
**接手已被選中的世界，處理它怎麼被治理。**

---

# 具體建議資料夾

我會直接這樣放：

```text
C:\ai_agent\volva\docs\INTENT\
```

或如果你想更貼互動分流：

```text
C:\ai_agent\volva\docs\ROUTING\
```

我個人偏 `INTENT`，因為比較貼 Völva 的角色。

---

# Thyra 這邊只需要留一個橋接文件

在 Thyra 裡不要複製這整批 spec。  
只留一份類似：

```text
C:\ai_agent\thyra\docs\THYRA\T0_VOLVA_HANDOFF.md
```

內容只寫：

- Völva 什麼情況可以把東西交給 Thyra
- handoff object 長什麼樣
- governance candidate 進 Thyra 前必備欄位是什麼

這樣兩邊邊界最乾淨。

---

# 如果你問的是 `midnight-market-canonical-slice.md` 這種檔案
那個就還是放 **Thyra**，不是 Völva。

所以邊界其實是：

## Völva
- intent
- routing
- space generation
- probe / commit

## Thyra
- canonical cycle
- world form instantiation
- change / judgment / pulse / outcome

---

如果你要，我下一步可以直接幫你：
1. 規劃 `C:\ai_agent\volva\docs\INTENT\` 的完整檔案樹
2. 寫 `T0_VOLVA_HANDOFF.md`

我建議先做 **2**。